public class Game extends JFrame{
	
	public static void main(String[] args){
		private Level mLevel; //eventually to be converted to Array of Level objects
		// use an ArrayDeque to store spaceship location history
		
	//load level, hardcode one or two Bodies for now
	
	
	//wait for click to arm launch
	
	//using a do/while 
	do {
		//get mouse position
		//show arrow from spaceship to mouse location
		
		if (clickIsReleased()){
			spaceship.launch(mouseX-spaceship.getX()
		
	} (while !isLaunched());
	
	}
	public void displayGame(Level level){
		//parse out objects
	}
}
